# Trail Race Analyzer v0.89 ONLINE FULL

## v0.89

### OCR
Исправлена ошибка `Can't find variable: raw`.

### Общая модель прогноза по 3 отдельным GPX
Во вкладке «Прогноз» теперь нужно отдельно загрузить:

1. **Силовая трейловая GPX** — обучает влияние крутых подъёмов/спусков.
2. **Быстрая трейловая GPX** — добавляет профиль скорости на быстром трейле и устойчивое усилие.
3. **Гоночная (плоская) GPX** — например, плоская десятка; задаёт абсолютную базовую скорость/темп.

Расчёт прогноза включается только когда:
- загружена GPX трассы гонки;
- успешно разобраны все 3 эталонные тренировочные GPX.

### Общая формула
`v = Vflat × Fgrade × FfastTrail × Ffatigue × Effort`

- `Vflat` берётся из Гоночной (плоской) GPX.
- `Fgrade` — смесь 65% Силовой трейловой + 35% Быстрой трейловой.
- `FfastTrail` — мягкая поправка по Быстрой трейловой.
- `Ffatigue` — общая оценка падения скорости по ходу дистанции из двух трейловых тренировок.
- `Effort` — выбранное относительное усилие, %.

**10.gpx подходит как третий файл очень хорошо.** Загруженный пример имеет около 10.13 км за 42:02, средний темп примерно 4:09/км и небольшой для сравнения с горной тренировкой набор (~142 м).

Реальных ватт Running Power в этих GPX нет, поэтому «мощность» остаётся относительным коэффициентом модели.

---

# Trail Race Analyzer v0.88 ONLINE FULL

## v0.88
- Исправлено распознавание расстояния на Garmin/Strava-скриншотах вида:
  - `Расстояние Набор высоты`
  - `16,16 км 2755 м`
- Теперь из такой пары извлекаются:
  - Расстояние = 16,16 км;
  - Набор = 2755 м.
- Расстояние автоматически попадает и в нижнее редактируемое поле.
- Если расстояние, время в движении, средний темп и средний пульс распознаны, процент становится 100%.
- Все функции v0.87, включая отдельный раздел «Прогноз на гонку», сохранены.

---

# Trail Race Analyzer v0.87 ONLINE FULL

## v0.87 — Прогноз на гонку по эталонной activity

Добавлена отдельная вкладка **«4. Прогноз»**, а «Итог» стал вкладкой 5.

### Модель
Встроенная калибровка построена по `activity_23810601185.gpx`:
- 17.13 км;
- около +2901 м;
- elapsed 6:09;
- 85 валидных 200-метровых участков.

Формула обучается по фактическому времени прохождения участков activity:

`ln(v) = a + b1·G+ + b2·G+² + b3·G− + b4·G−² + b5·progress`

где:
- `v` — прогнозная скорость, м/с;
- `G+` — положительный уклон;
- `G−` — отрицательный уклон;
- `progress` — положение от старта к финишу (модель усталости).

Коэффициенты встроенной калибровки:
`[0.761443, -4.950940, 3.488795, 3.465680, -14.162378, -0.348810]`.

### Относительная мощность
Добавлено поле **«Относительная мощность / усилие, %»**.
Это не физические ватты:
- 100% = усилие как в эталонной activity;
- 95% = скорость модели ×0.95;
- 105% = скорость модели ×1.05.

### Прогноз
По GPX гонки приложение:
- режет трассу на короткие ~200 м отрезки;
- считает локальный уклон;
- прогнозирует скорость каждого отрезка;
- объединяет результат в таблицу по 1–10 км;
- показывает набор/сброс, уклон, темп, время отрезка и накопленное время;
- выводит прогноз финиша, средний темп и диапазон ±10%.

Можно загрузить другую timed-GPX activity и перекалибровать формулу прямо в браузере.

Для `50.gpx` при встроенной калибровке и усилии 100% модель ориентировочно даёт около **9 часов движения**; точное значение в приложении зависит от текущего сглаженного профиля GPX.

---

# Trail Race Analyzer v0.86 ONLINE FULL

## v0.86
- В раздел «Итог» добавлена **Пульсовая стратегия**.
- Стратегия строится автоматически из:
  - длины GPX трассы;
  - среднего пульса «Лучшей тренировки».
- Гонка делится на 4 участка:
  - начало — контролируемый пульс;
  - середина — рабочий горный диапазон;
  - поздняя часть — постепенное повышение;
  - последние ~5% — финиш без экономии при хорошем состоянии.
- На спусках приложение отдельно напоминает не удерживать высокий пульс искусственно.
- При смене фото/очистке тренировки старая пульсовая стратегия очищается.
- Все изменения v0.85 сохранены.

---

# Trail Race Analyzer v0.85 ONLINE FULL

- Добавлена подпись: «Среднее время ожидания OCR — 56 секунд».
- Максимальное время ожидания OCR остаётся 60 секунд.

# Trail Race Analyzer v0.84 ONLINE FULL

## v0.84
Исправления раздела «Лучшая тренировка»:

- После выбора файла и нажатия «Загрузить и распознать» выбранный скриншот больше не сбрасывается.
- Имя выбранного файла дополнительно показывается отдельной строкой и остаётся видно после OCR.
- При выборе новой фотографии очищаются только старые результаты и поля тренировки, но новый выбранный файл сохраняется.
- При обновлении страницы поля принудительно очищаются несколько раз:
  - сразу после загрузки DOM;
  - после первого кадра;
  - через 120 мс;
  - через 500 мс;
  - при Safari `pageshow`.
  Это сделано потому, что Safari может восстанавливать значения формы уже после `DOMContentLoaded`.
- На настоящем обновлении страницы также очищается выбор файла.
- OCR timeout 60 секунд сохранён.

---

# Trail Race Analyzer v0.83 ONLINE FULL

## v0.83
- Удалён второй дублирующий блок «Скриншот тренировки» с кнопкой «Загрузить и обработать скриншот».
- Оставлен только верхний основной OCR-блок.
- Удалена старая кнопка «Заполнить из текста», относившаяся к дублирующему блоку.
- Поля «Лучшая тренировка» теперь принудительно очищаются:
  - при обычном обновлении страницы;
  - при повторном открытии страницы из Safari back/forward cache;
  - при выборе новой фотографии.
- Очищаются: Расстояние, Время в движении, Средний темп, Средний пульс, OCR-процент и время обработки.
- У полей отключено браузерное автозаполнение.
- Лимит OCR 60 секунд сохранён.

---

# Trail Race Analyzer v0.82

Исправлено:
- «Время в движении» после OCR теперь записывается в правильное поле.
- Видимые поля «Лучшая тренировка»: Расстояние, Время в движении, Средний темп, Средний пульс.
- «Набор», «Макс. пульс», LTHR убраны из интерфейса.
- Значения `1` убраны: поля стартуют пустыми.
- Новая фотография/обновление страницы очищает поля.
- Формат времени `47:14` сохраняется, для расчётов преобразуется в минуты.
- OCR timeout 60 секунд сохранён.






