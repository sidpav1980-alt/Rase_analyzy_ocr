# Trail Race Analyzer v0.77 ONLINE FULL

## v0.77
- Исправлен парсер OCR Garmin/Strava.
- Теперь учитываются пары заголовков и значений, например:
  - `Расстояние Средний темп` → `8,03 км 5:53 /км`;
  - `Время в движении Набор высоты` → `47:14 17 м`;
  - `Калории Сред. пульс` → `636 Ккал 155 уд/мин`.
- Число `8,03 км` больше не должно ошибочно превращаться в темп `8:03`.
- Для такого скриншота ожидается:
  - Расстояние: 8,03 км;
  - Время в движении: 47:14;
  - Средний темп: 5:53 /км;
  - Средний пульс: 155 уд/мин.
- При распознавании всех четырёх основных показателей процент = **100%**.
- Полностью убран нижний блок ручного ввода данных тренировки.
- Распознанные данные сохраняются в `state.bestTraining` и используются моделью без ручных полей.
- Неограниченный OCR и предупреждение после 60 секунд из v0.76 сохранены.

---

# Trail Race Analyzer v0.76 ONLINE FULL

## v0.76
- OCR по-прежнему не имеет искусственного таймаута и не прерывается.
- Если распознавание идёт дольше 60 секунд, появляется предупреждение:
  `Распознавание занимает необычно много времени. OCR продолжает работать.`
- Предупреждение автоматически исчезает после завершения OCR или ошибки.
- Счётчик времени обработки продолжает работать всё время распознавания.

---

# Trail Race Analyzer v0.75 ONLINE FULL

## v0.75
- Убраны все искусственные ограничения времени OCR.
- Браузер больше не прерывает запрос через 30 секунд.
- Tesseract на сервере больше не ограничен 24 секундами.
- Индикатор «Время обработки» продолжает считать фактически затраченное время до завершения OCR.
- При реальной ошибке сервера отображается сама ошибка, без искусственного client-side timeout.

---

# Trail Race Analyzer v0.74 ONLINE FULL

## v0.74
Собрано заново на базе **v0.64**.

Изменения:
- На основной странице оставлен только ручной ввод **ITRA PI**, по умолчанию `0`.
- Поле ITRA profile / Runner ID и кнопка загрузки собственного ITRA удалены.
- AI для стартового списка сохранён.
- Убрано «Описание покрытия».
- «Тренировки» переименованы в **«Лучшая тренировка»**.
- Лучшая тренировка ориентируется на высокий средний пульс и лучшие показатели дистанции, набора и темпа.
- OCR: жёсткий лимит браузера **30 секунд**, серверный Tesseract около **24 секунд**.
- При timeout таймер останавливается, остаются `0%` и затраченное время.
- Названия результатов OCR:
  - Расстояние;
  - Время в движении;
  - Средний темп;
  - Средний пульс.
- Заголовочный текст изменён на:
  `Загрузи GPX, лучшую тренировку и стартовый список. Приложение рассчитает стратегию, прогноз финиша и шансы против соперников.`

---

# Trail Race Analyzer v0.64 ONLINE FULL

## v0.64
- В разделе «Тренировки» оставлен один основной блок загрузки скриншота.
- Кнопка переименована в **«Загрузить и распознать»**.
- Добавлен таймаут OCR:
  - 45 секунд в браузере;
  - 35 секунд у Tesseract на сервере.
- Большие изображения уменьшаются перед OCR, маленькие увеличиваются.
- Добавлена подготовка изображения в grayscale + threshold.
- После OCR приложение автоматически пытается распознать и показать:
  - дистанцию;
  - набор;
  - время;
  - темп;
  - средний пульс.
- Найденные дистанция, набор и средний пульс автоматически подставляются в существующие поля тренировки.
- Новый скриншот очищает старый OCR и разобранные показатели.

---

# Trail Race Analyzer v0.63 ONLINE FULL

## v0.63
- Исправлена ошибка OCR `tesseract is not installed or it's not in your PATH`.
- Для Render добавлен **Dockerfile**, который устанавливает:
  - `tesseract-ocr`;
  - `tesseract-ocr-rus`;
  - `tesseract-ocr-eng`.
- Render-конфигурация переключена на `runtime: docker`.
- Добавлен endpoint `/api/ocr-health`.
- Перед распознаванием приложение проверяет, установлен ли OCR на сервере.
- Если OCR отсутствует, показывается понятное сообщение вместо длинной ошибки pytesseract.
- Для небольших скриншотов добавлено увеличение изображения перед OCR.
- Основной режим OCR: `rus+eng`, `--psm 6`.

## Как деплоить на Render
Создайте/пересоздайте **Web Service** из репозитория с этой версией.
Render должен определить `Dockerfile` и собирать сервис как Docker.

После деплоя откройте:
`/api/ocr-health`

Ожидаемый результат: `"ok": true`.

---

# Trail Race Analyzer v0.62 ONLINE FULL

## v0.62
- В «Итог» прогноз финиша больше не заполняется, если анализировать нечего.
- Для расчёта нужен GPX и хотя бы один дополнительный источник данных:
  - ITRA PI > 0;
  - данные тренировки;
  - стартовый список.
- Если данных недостаточно, прогноз финиша, место и вероятности показываются как `—`.
- Добавлены отдельные вероятности:
  - TOP-3;
  - TOP-10;
  - TOP-30;
  - TOP-50;
  - Победа.
- TOP-10/30/50 считаются непосредственно в тех же 10 000 Monte Carlo симуляциях, что и TOP-3.
- При выборе нового GPX старый итог очищается.

---

# Trail Race Analyzer v0.61 ONLINE FULL

## v0.61
- В разделе «Тренировки» добавлена загрузка одного скриншота.
- Кнопка «Распознать текст со скриншота» запускает OCR.
- Распознанный русский/английский текст выводится в редактируемое поле.
- Новый скриншот очищает предыдущий результат.
- Кнопка зелёная при успехе и красная при ошибке.
- Добавлен `/api/training-ocr` и зависимости Tesseract/Pillow/pytesseract.

## Render
Для OCR серверу нужен Tesseract с русским языковым пакетом. Добавлен `Aptfile`.
Если текущий Render runtime не устанавливает Aptfile, для OCR понадобится Docker deployment.

---

# Trail Race Analyzer v0.60 ONLINE FULL

## v0.60
- Исправлено получение собственного ITRA PI по ссылке RunnerSpace.
- PI больше не определяется как «любое трёхзначное число на странице».
- Сервер сначала ищет явные JSON-поля Performance Index.
- Для HTML анализируется только узкий блок между `ITRA Performance Index` и `General Stats`/следующим разделом.
- Числа из рейтинга, Runner ID, результатов гонок и других частей страницы больше не могут стать PI.
- Если ITRA не отдаёт Performance Index, приложение ставит PI = **0** и показывает ошибку.
- Для собственного профиля ИИ/OpenRouter не используется.
- Поддерживается старый URL вида:
  `https://itra.run/RunnerSpace/sidorenko.pavel.1840251`
  и современный формат RunnerSpace с отдельным numeric Runner ID.

Важно: ITRA может отвечать серверному запросу 403. В этом случае приложение не выдумывает PI и оставляет 0.

---

# Trail Race Analyzer v0.59 ONLINE FULL

## v0.59
- Поле **«Базовый темп»** переименовано в **«Рассчитать время для темпа, мин/км»**.
- Блок **«Время прохождения»** переименован в **«Фактическое время прохождения»**.
- Фактическое время и темп по-прежнему берутся из временных меток GPX.
- Расчётное поле темпа остаётся отдельным и не влияет на фактическое время GPX.

---

# Trail Race Analyzer v0.58 ONLINE FULL

## v0.58
- К времени прохождения из GPX добавлен средний темп.
- **Без пауз** показывает время движения и темп движения.
- **С паузами** показывает полное elapsed time и средний темп с учётом пауз.
- Оба темпа рассчитываются по фактической длине загруженного GPX.
- Формат темпа: `мин:сек /км`.

---

# Trail Race Analyzer v0.57 ONLINE FULL

## v0.57
- «Время прохождения» больше не рассчитывается из базового темпа.
- Оба времени берутся непосредственно из временных меток загруженного GPX.
- **С паузами** = время от первой до последней временной точки GPX.
- **Без пауз** = сумма интервалов фактического движения; стоянки и GPS-дрожание исключаются.
- Поле «Паузы, мин» удалено — паузы определяются по самому GPX.
- Если GPX не содержит `<time>`, приложение показывает `нет времени в GPX`.
- Базовый темп остаётся отдельным параметром для остальных прогнозных расчётов.

---

# Trail Race Analyzer v0.56 ONLINE FULL

## v0.56
- Над блоком «Анализ карты» добавлены две строки времени:
  - **Время прохождения без пауз**;
  - **Время прохождения с паузами**.
- Без пауз считается по текущему базовому темпу и правилу `+1000 м набора = +1 час`.
- Добавлено поле **«Паузы, мин»** рядом с базовым темпом.
- Время с паузами = расчётное время без пауз + введённые минуты остановок.
- Значения обновляются при изменении темпа или пауз.
- При выборе нового GPX значения времени сбрасываются до обработки.

---

# Trail Race Analyzer v0.55 ONLINE FULL

## v0.55
- Собственный ITRA PI теперь загружается **без ИИ/OpenRouter**.
- Для кнопки «Загрузить мой ITRA по ссылке» обязательно указывается:
  - ссылка на RunnerSpace, или
  - Runner ID.
- Сервер открывает только указанный профиль/Runner ID и пытается получить Performance Index напрямую.
- Если PI не найден в указанном профиле, возвращается ошибка; поиск по ФИО и AI-fallback для собственного спортсмена больше не запускаются.
- OpenRouter остаётся только для анализа стартового списка конкурентов.

---

# Trail Race Analyzer v0.54 ONLINE FULL

## v0.54
- Если трек пересекает воду в месте, где в OpenStreetMap отмечен мост, это пересечение больше не считается бродом.
- Такое место выводится отдельной строкой: `Пересечение воды по мосту на км: ...`.
- Поле «Броды» показывает только количество настоящих бродов.
- Ниже остаётся строка `Броды на км: ...`.
- Проверка моста сделана отдельно от загрузки карты, чтобы не ломать OSM-анализ.
- При загрузке нового GPX и при обновлении страницы строки бродов/мостов очищаются.
- Для сопоставления моста используется допуск около 35 м.

Важно: если мост есть на местности, но не отмечен в OSM, приложение может считать такое пересечение бродом.

---

# Trail Race Analyzer v0.53 ONLINE FULL

## v0.53
- Исправлено: анализ карты больше не восстанавливается после обновления страницы.
- При каждом новом открытии/refresh очищаются:
  - проценты покрытия;
  - болото / вода / тропа / грунт / асфальт;
  - количество бродов;
  - километры бродов;
  - цветная полоса покрытия;
  - статус и прогресс анализа карты.
- `trailMapAnalysis` удаляется из localStorage при старте страницы.
- Для получения анализа после refresh нужно снова обработать GPX и нажать «Анализ карты».

---

# Trail Race Analyzer v0.52 ONLINE FULL

## v0.52
- При выборе нового GPX очищается собственный ITRA:
  - ITRA PI сбрасывается в **0**;
  - ссылка / Runner ID очищается;
  - локально сохранённый собственный ITRA-профиль удаляется;
  - статус кнопки возвращается в начальное состояние.
- Исправлена ошибочная загрузка PI вроде **956**.
- Сервер больше не принимает произвольное трёхзначное число со страницы как ITRA PI.
- PI извлекается только из явных полей `Performance Index`, `ITRA PI` или соответствующих JSON-полей.
- AI-fallback стал консервативнее: требует источник `itra.run` и не принимает подозрительные значения 900+.
- PI по умолчанию остаётся **0**.

---

# Trail Race Analyzer v0.51 ONLINE FULL

## v0.51
- Исправлено отображение блока **«Броды»**.
- В большом поле «Броды» теперь показывается только **количество бродов**: например `2`.
- Ниже отдельно остаётся километраж: `Броды на км: 21.2, 30.8`.
- Список координат/километров больше не выводится вместо количества.

---

# Trail Race Analyzer v0.50 ONLINE FULL

## v0.50
- Исправлена ошибка анализа карты `Can't find variable: countLikelyFords`.
- Причина: после изменения алгоритма бродов функция была переименована в `findLikelyFords`, но в одном месте остался вызов старого имени.
- Все старые вызовы `countLikelyFords` заменены.
- Проверка мостов остаётся отключённой.
- Количество бродов и километры бродов сохраняются.

---

# Trail Race Analyzer v0.48 ONLINE FULL

## v0.48
- Исправлена загрузка собственного ITRA по ссылке вида:
  `https://itra.run/RunnerSpace/sidorenko.pavel.1840251`.
- Из старого формата ссылки автоматически извлекаются slug и Runner ID.
- Дополнительно строится современный кандидат URL вида:
  `/RunnerSpace/SIDORENKO.Pavel/1840251`.
- Если RunnerSpace загружает данные через внутренний `api/RunnerSpace/GetRunnerSpace`,
  сервер пытается найти этот endpoint в HTML и получить PI оттуда.
- Если Runner ID/ссылка указаны, ФИО не требуется и обычный поиск по ФИО не запускается первым.
- ITRA PI по умолчанию остаётся 0.

### Анализ карты / броды
- Отменена логика «если рядом отмечен мост — не считать бродом».
- Удалена проверка `bridge=yes`, так как она усложняла анализ карты и могла ломать загрузку.
- Броды снова определяются только как короткие пересечения водного участка.
- Километраж бродов сохраняется.

---

# Trail Race Analyzer v0.47 ONLINE FULL

## v0.47
- Внизу приложения добавлена подпись: `Sidorenko Pavel (c) 2026`.
- Подпись центрирована и учитывает нижнюю safe area на iPhone.

---

# Trail Race Analyzer v0.46 ONLINE FULL

## v0.46
- Броды теперь показываются не только количеством, но и километражем: например `2.4, 11.7 км`.
- Если в OSM рядом с пересечением отмечен мост (`bridge=yes`), такое пересечение не считается бродом.
- Длинные участки движения вдоль воды по-прежнему не считаются бродом.
- Если указан ITRA Runner ID или ссылка на профиль, для «Загрузить мой ITRA» ФИО больше не обязательно.
- При указанном Runner ID поиск сначала выполняется именно по ID/ссылке, без запроса ФИО.
- ITRA PI по умолчанию теперь **0**.

Важно: определение бродов зависит от полноты OSM. Не отмеченный в OSM мост приложение не сможет исключить автоматически.

---

# Trail Race Analyzer v0.45 ONLINE FULL

## v0.45
- В «Анализ карты» добавлен показатель **Броды**.
- Приложение считает отдельные короткие пересечения участков, классифицированных OSM как вода/река.
- Несколько соседних точек воды объединяются в один брод.
- Длинный участок вдоль воды (>300 м) автоматически не считается бродом.
- При выборе нового GPX счётчик бродов очищается вместе с остальным анализом покрытия.
- Это оценка по данным OSM, а не гарантия фактического брода на местности.

---

# Trail Race Analyzer v0.44 ONLINE FULL

## v0.44
- «Базовый темп, мин/км» перенесён непосредственно к выбору GPX в разделе «Трасса».
- Поле расположено рядом с блоком загрузки трассы.
- На очень узком экране автоматически переносится вниз.
- Расчётная логика поля не изменена.

---

# Trail Race Analyzer v0.43 ONLINE FULL

**Текущая версия: v0.43 ONLINE FULL**

## v0.43
- В разделе «Трасса» добавлено поле **ITRA профиль / Runner ID**.
- Можно вставить прямую ссылку на свой профиль ITRA.
- Кнопка **«Загрузить мой ITRA»** сначала пытается взять PI непосредственно из указанного профиля.
- Если прямой профиль не дал PI, включается fallback:
  - поиск по Runner ID / slug;
  - поиск по имени;
  - AI-сопоставление через OpenRouter.
- Это значительно надёжнее поиска только по имени.
- Найденная ссылка/Runner ID сохраняется локально вместе с собственным ITRA PI.

---

# Trail Race Analyzer v0.42 ONLINE FULL

**Текущая версия: v0.42 ONLINE FULL**

## v0.42
- При выборе **нового GPX** полностью очищается анализ покрытия предыдущей трассы.
- Сбрасываются:
  - процент классифицированного маршрута;
  - болото;
  - вода;
  - тропа;
  - грунт;
  - асфальт;
  - текст результата анализа;
  - цветная полоса покрытия;
  - прогресс анализа карты.
- Старый `trailMapAnalysis` удаляется из localStorage.
- Кнопка «Анализ карты» снова блокируется до успешной обработки нового GPX.
- После обработки нового GPX кнопка снова становится активной.

---

# Trail Race Analyzer v0.41 ONLINE FULL

**Текущая версия: v0.41 ONLINE FULL**

## v0.41
- В блок **«Анализ карты»** добавлена понятная легенда цветов покрытия.
- Расшифровка:
  - зелёный — болото / wetland;
  - голубой — вода / река;
  - фиолетовый — тропа;
  - оранжево-коричневый — грунт / гравий / земля;
  - серый — асфальт / твёрдое покрытие;
  - тёмно-серый — неизвестно / нет данных OSM.
- Легенда адаптирована под iPhone и на узком экране показывается в одну колонку.

---

# Trail Race Analyzer v0.40 ONLINE FULL

**Текущая версия: v0.40 ONLINE FULL**

## v0.40
- В раздел **«Трасса»** добавлена кнопка **«Загрузить мой ITRA»**.
- Кнопка берёт имя спортсмена из поля сверху.
- Выполняется тот же онлайн-поиск ITRA, что и для стартового списка.
- При успешном поиске найденный ITRA PI автоматически подставляется в поле спортсмена.
- Цвет кнопки:
  - синяя — готова;
  - серая — идёт поиск;
  - зелёная — PI найден и загружен;
  - красная — ошибка / профиль не найден.
- Найденный собственный профиль сохраняется локально на iPhone.
- Ручной OpenRouter API key из v0.38 также используется для этого запроса, если он задан.

---

# Trail Race Analyzer v0.39 ONLINE FULL

**Текущая версия: v0.39 ONLINE FULL**

## v0.39
- Исправлена неактивная кнопка **«Анализ карты»**.
- После успешной обработки GPX кнопка автоматически становится активной и синей.
- При выборе нового GPX кнопка снова блокируется до обработки нового файла.
- При ошибке GPX анализ карты остаётся заблокирован.
- Добавлена отдельная функция синхронизации состояния кнопки, чтобы её состояние не зависело от порядка старых обработчиков.
- Сохранены Map Proxy, OSM-анализ, ITRA/OpenRouter, PI 548 и ручной ввод API key.

---

# Trail Race Analyzer v0.38 ONLINE FULL

**Текущая версия: v0.38 ONLINE FULL**

## v0.38
- Добавлен индикатор состояния OpenRouter:
  - `Render подключён ✓`;
  - `ключ на iPhone ✓`;
  - `ключ не настроен ✕`.
- Добавлено ручное поле для OpenRouter API key.
- Ключ можно сохранить локально на конкретном iPhone.
- Ручной ключ передаётся только при запросе ITRA и используется сервером для этого запроса.
- Если ручного ключа нет, сервер использует `OPENROUTER_API_KEY` из Render.
- Добавлена кнопка удаления локального ключа.
- Ключ не добавляется в исходный код приложения и не записывается в README.

---

# Trail Race Analyzer v0.37 ONLINE FULL

**Текущая версия: v0.37 ONLINE FULL**

## v0.37
Объединены онлайн-функции в одной сборке:

- GPX-анализ.
- Профиль высоты по километражу.
- Профиль высоты по расчётному времени.
- Trail Difficulty 0–10.
- ITRA Km-effort / Endurance Points.
- Анализ карты через Render proxy → Overpass.
- Определение болот, воды, троп, грунта и асфальта по OSM.
- Онлайн-поиск ITRA PI участников стартового списка.
- AI-сопоставление ITRA через OpenRouter Free.
- ITRA PI спортсмена по умолчанию: **548**.

## ITRA button
На странице стартового списка:
- синяя — готова;
- серая — запрос выполняется;
- зелёная — ITRA успешно загружен;
- красная — ошибка.

## Render
Запускать как **Web Service**.

Build:
`pip install -r requirements.txt`

Start:
`gunicorn server:app --bind 0.0.0.0:$PORT`

Environment:
- `OPENROUTER_API_KEY` — обязательно для ITRA поиска;
- `OPENROUTER_MODEL` = `openrouter/free`;
- `SITE_URL` — опционально.

## Health
`/health`










